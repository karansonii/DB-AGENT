environment: dev

mysql:
  host: localhost
  port: 3307
  user: root
  password: devpass
  db: egp_dev

postgres:
  host: localhost
  port: 5432
  user: postgres
  password: devpass
  db: central_repo_dev

qdrant:
  host: localhost
  port: 6334
  collection: knowledge
